# ----------------------------------------------------------------------------------------------------------------------
# Код принадлежит: (впишите сюда ваш ник или кличку)
# Версия кода: 0.1v
#
# Build for PC: pyinstaller -F --onefile -n NameOutputFile main.py
#
# Для работы с кодом установите эти библиотеки (просто впишите в консоль):
# pip install pyinstaller
# pip install pygame
# ----------------------------------------------------------------------------------------------------------------------

# Библиотеки: ----------------------------------------------------------------------------------------------------------
if True:
    import sys
    import pygame
    from pygame.locals import *

    pygame.init()
# ----------------------------------------------------------------------------------------------------------------------

# Переменные: ----------------------------------------------------------------------------------------------------------
if True:
    # Окно:
    if True:
        FPS = 60
        title = "Window"
        title_icon = None  # pygame.image.load("dir/icon.png")  # .convert() | .convert_alpha()
        w_size, h_size = 960, 540

    # Другое:
    if True:
        DeltaTime = 0  # Умножать все вычисления(перемещение, прыжок, ...) на эту переменную.
# ----------------------------------------------------------------------------------------------------------------------

# Окно: ----------------------------------------------------------------------------------------------------------------
if True:
    # pygame.display.set_icon(title_icon)
    pygame.display.set_caption(title)
    screen = pygame.display.set_mode((w_size, h_size), RESIZABLE)
    clock = pygame.time.Clock()
# ----------------------------------------------------------------------------------------------------------------------

# Классы и функции: ----------------------------------------------------------------------------------------------------
if True:
    pass
# ----------------------------------------------------------------------------------------------------------------------

# Цикл: ----------------------------------------------------------------------------------------------------------------
while True:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            pygame.quit()  # --- Закрытие окна.
            sys.exit()  # ------ Закрытие программы из фона.
    
    # Пре-цикличное обновление: ----------------------------------------------------------------------------------------
    if True:
        screen.fill((0, 0, 0))  # Очистка экрана.
        
        # Получение времени между кадрами:
        if clock.get_fps() != 0:
            DeltaTime = FPS / clock.get_fps()
    # ------------------------------------------------------------------------------------------------------------------

    # Основное: --------------------------------------------------------------------------------------------------------
    if True:
        pass
    # ------------------------------------------------------------------------------------------------------------------

    # Отрисовка: -------------------------------------------------------------------------------------------------------
    if True:
        # [...]

        pygame.display.update()
    # ------------------------------------------------------------------------------------------------------------------

    clock.tick(FPS)
# ----------------------------------------------------------------------------------------------------------------------
