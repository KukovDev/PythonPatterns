# ----------------------------------------------------------------------------------------------------------------------
# Код принадлежит: Dev.
# Лицензия: "License".
# Версия кода: 1.0v.
# Build for PC: pyinstaller -F --onefile -n Program main.pyw
# ----------------------------------------------------------------------------------------------------------------------

# Библиотеки: ----------------------------------------------------------------------------------------------------------
if True:
    import sys
    import pygame  # ------------- pip install pygame
    from pygame.locals import *

    pygame.init()
# ----------------------------------------------------------------------------------------------------------------------

# Переменные: ----------------------------------------------------------------------------------------------------------
if True:
    # Окно:
    if True:
        FPS = 60
        vSync = True
        title = "Window"
        title_icon = None  # pygame.image.load("dir/icon.png")  # .convert() | .convert_alpha()
        w_size, h_size = 960, 540

    # Другое:
    if True:
        DeltaTime = 0
# ----------------------------------------------------------------------------------------------------------------------

# Окно: ----------------------------------------------------------------------------------------------------------------
if True:
    screen = pygame.display.set_mode((w_size, h_size), RESIZABLE, vsync=0)
    if vSync:
        pygame.display.set_mode((screen.get_width(), screen.get_height()), RESIZABLE, vsync=1)
    # pygame.display.set_icon(title_icon)
    pygame.display.set_caption(title)
    clock = pygame.time.Clock()
# ----------------------------------------------------------------------------------------------------------------------

# Классы и функции: ----------------------------------------------------------------------------------------------------
if True:
    pass
# ----------------------------------------------------------------------------------------------------------------------

# Цикл: ----------------------------------------------------------------------------------------------------------------
while True:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            pygame.quit()  # --- Закрытие окна.
            sys.exit()  # ------ Закрытие программы из фона.
    screen.fill((0, 0, 0))
    
    # Пре-цикличное обновление: ----------------------------------------------------------------------------------------
    if True:
        pass
    # ------------------------------------------------------------------------------------------------------------------

    # Основное: --------------------------------------------------------------------------------------------------------
    if True:
        pass
    # ------------------------------------------------------------------------------------------------------------------

    # Отрисовка: -------------------------------------------------------------------------------------------------------
    if True:
        pass
    # ------------------------------------------------------------------------------------------------------------------

    pygame.display.update()
    DeltaTime = clock.tick(FPS) / 16
# ----------------------------------------------------------------------------------------------------------------------
